synapterGUI <- function(n = 1) {
    .Defunct("synergise", package = "synapter",
             msg = c("The synapterGUI() interface is defunct.\n",
                     "Please use synergise() instead."))
}
